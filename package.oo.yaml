version: 1.1.1
scripts:
  # Script executed when the container is first loaded
  # eg: bootstrap: pip install pandas
  bootstrap: |
    pnpm install && pip install -r requirements.txt
icon: "./icon.png"
dependencies:
  oomol-preview: 0.1.7
  oomol-file: 0.0.20
name: hello-world
description: "%this-is-a-demonstration-project-for-the-oomol-platform-that-show%"
displayName: "%hello-world%"
repository: https://github.com/oomol-flows/hello-world
releaseNotes: fix bug
